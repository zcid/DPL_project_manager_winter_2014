<%= bootstrap_form_for [@project, @task] do |f| %>

  <%= f.text_field :title %>

  <%= f.collection_select(:user_id, @users, :id, :email) %>

  <%= f.text_area :description %>

  <%= f.date_field :deadline %>

  <%= f.check_box :is_completed, class: 'tog' %>

  <%= f.submit %>
<% end %>